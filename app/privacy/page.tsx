import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Политика конфиденциальности', robots: { index: false } };

function contactEmail(): string {
  const url = process.env.NEXT_PUBLIC_SITE_URL ?? '';
  const domain = url.replace(/^https?:\/\//, '').replace(/^www\./, '').split('/')[0];
  const ok = domain.includes('.') && !domain.startsWith('localhost') && !domain.startsWith('127.');
  return `privacy@${ok ? domain : 'example.com'}`;
}

export default function PrivacyPage() {
  const contact = contactEmail();
  return (
    <div className="container legal">
      <h1>Политика конфиденциальности</h1>
      <p>Простыми словами: что мы собираем, зачем и как это удалить. Без юридических сложностей.</p>
      <p>
        <strong>Что хранится на вашем устройстве.</strong> Ваши списки, история просмотров, закладки и настройки
        живут в localStorage вашего браузера. Они не уходят на наши серверы, пока вы сами не нажмёте
        «Отправить локальные данные» в профиле.
      </p>
      <p>
        <strong>Что хранится на сервере, если вы вошли в аккаунт.</strong> Email и пароль (в зашифрованном виде),
        ваши списки и история для синхронизации между устройствами, отзывы и комментарии с именем, которое вы
        указали, и подписка на push-уведомления (адрес браузера и ключи шифрования).
      </p>
      <p>
        <strong>Технические записи.</strong> Как и любой сайт, мы видим IP-адрес и тип браузера в логах запросов.
        Это нужно только для защиты от спама и поломки сайта. Логи хранятся недолго и никому не передаются.
      </p>
      <p>
        <strong>Куки и сторонние сервисы.</strong> Мы используем Cloudflare Turnstile, чтобы отличать людей от
        ботов при отправке отзывов. Постеры приходят с CDN AniList и Shikimori — эти сервисы видят ваш IP при
        загрузке картинок, если прокси изображений выключен.
      </p>
      <p>
        <strong>Как удалить свои данные.</strong> В профиле: «Выйти» очищает ключи входа на устройстве;
        «Очистить историю» удаляет просмотры. Чтобы полностью удалить аккаунт и все данные с сервера, напишите
        нам на {contact} — удалим в течение 7 дней.
      </p>
      <p>
        <strong>Мы не продаём данные.</strong> Никакой рекламы, трекеров и передачи данных третьим сторонам
        помимо случаев, описанных выше. Вопросы по политике — на {contact}.
      </p>
    </div>
  );
}
